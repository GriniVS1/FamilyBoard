{
  "name": "familyboard-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker relay: phones reach the wall's mobile API from outside the LAN through a device-initiated WebSocket tunnel.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
