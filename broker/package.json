{
  "name": "familyboard-oauth-broker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker OAuth broker for FamilyBoard (Google/Microsoft calendar linking on LAN devices).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240000.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
