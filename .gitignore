node_modules
.next
out
dist
.DS_Store
**/.DS_Store
.env
.env.local
.env.*.local
data/
*.tsbuildinfo
next-env.d.ts
.vercel
coverage
.claude/settings.local.json

# Service-account JSON, OAuth refresh tokens, anything sensitive
secrets/
*.private.json
*-service-account*.json

# Legacy prototype files that hold live Google API keys — never commit.
# Use config.js.example as the template. (A real key was leaked publicly from
# config.js once; keep these out of git.)
/config.js
/index.html

# Vendor private keys — NEVER commit
tool/.license-private.pem
tool/.release-private.pem
tool/*.pem
# …but the OTA release PUBLIC key is meant to be committed and baked into the image
!tool/release-pub.pem
